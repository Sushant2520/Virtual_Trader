import React,{useEffect,useState} from 'react'; import API from '../api/api'; export default function History(){ const [trades,setTrades]=useState([]); useEffect(()=>{ API.get('/trades/history').then(r=>setTrades(r.data.trades)).catch(()=>{}); },[]); return (<div style={{maxWidth:900,margin:'20px auto'}}><h2>Trade History</h2>{trades.map(t=> (<div key={t.tradeId} style={{border:'1px solid #eee',padding:8,marginBottom:8}}>{t.symbol} {t.side} x{t.quantity} @ {t.price}</div>))}</div>); }
